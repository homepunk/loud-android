<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowBackground">@color/transparent</item>
        <!--USED FOR TRANSPARENT BOTTOM NAVIGATION-->
        <item name="android:navigationBarColor">@color/background</item>
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="FieldTextStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/whiteBone</item>
        <item name="android:fontFamily">@font/lato</item>
        <item name="android:textSize">@dimen/text_size_card</item>
    </style>

    <style name="BlockTextStyle" parent="FieldTextStyle">
        <item name="android:textSize">@dimen/text_size_default</item>
        <item name="android:layout_marginStart">@dimen/margin_big_20dp</item>
        <item name="android:layout_marginEnd">@dimen/margin_big_20dp</item>
    </style>

    <style name="AppModeEventTheme" parent="AppTheme">
        <item name="colorAccent">@color/modeEventsColor</item>
    </style>

    <style name="AppModeLibraryTheme" parent="AppTheme">
        <item name="colorAccent">@color/modeLibraryColor</item>
    </style>

    <style name="AppModeGearTheme" parent="AppTheme">
        <item name="colorAccent">@color/modeGearColor</item>
    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.3</item>

    </style>
</resources>
